database:
  host: mysql
  port: 3306
  user: template_user
  password: template_pass
  name: template_db
  charset: utf8mb4

openai:
  api_key: "${OPENAI_API_KEY}"
  base_url: "${OPENAI_BASE_URL}"
  model: "${OPENAI_MODEL}"

app:
  debug: true
  name: project-template
  heartbeat_message: template alive
